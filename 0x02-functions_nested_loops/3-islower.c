#include "main.h"

/**
 * _islower - checks for a lowercase character
 * Return 0 if lowercase 1 if otheriwse
 */
int _islower(int c)
{
	int a;

	c = (c + '0');
	if (c >= 97 && <= 122)
	{
		a = 0
	}
	else
		a = 1
return (a);

}
