#include "main.h"
#include <stdio.h>

/**
 * add - This functions adds two integers and returns the result
 * @a: First operand
 * @b: second operand
 *
 * Return: Sums the operands @a and @b together
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
