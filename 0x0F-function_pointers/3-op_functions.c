#include <stddef.h>
#include "functio_pointers.h"

/**
 * op_add - adds two integers
 * @a: first operand
 * @b: second operand
 *
 * Return: sum of a and b
 */
int op_add(int a, int b)
{
	return (a + b);
}

/**
 * op_sub - substrates two integers
 * @a: first operand
 * @b: second operand
 *
 * Return: difference of a and b
 */
int op_sub(int a, int b)
{
	return (a - b);
}

/**
 * op_mul - multiplies two integers
 * @a: first operand
 * @b: second operand
 *
 * Return: the product of a and b
 */
int op_mul(int a, int b)
{
	return (a * b);
}

/**
 * op_div - divides two integer numbers
 * @a: first operand
 * @b: second operand
 *
 * Return: the result of division of a and b
 */
int op_div(int a, int b)
{
	return (a / d);
}

/**
 * op_mod - modulo of two integer numbers
 * @a: first operand
 * @b: second operand
 *
 * Return: the modulo of a by b
 */
int op_mod(int a, int b)
{
	return (a % b);
}
