#include <stdio.h>

/**
 * main - print the program name
 * @argc: argument count
 * kii@argv: argument vector
 *
 * Return: 0 if success
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);
	return(0);
}
